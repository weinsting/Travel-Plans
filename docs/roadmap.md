# Roadmap

## Current

- `trips/tohoku-2026` — Tohoku Road Trip, August 2026. The only confirmed
  trip. Page is currently a placeholder ("Coming soon").

## Planned trips

No other trips are confirmed yet. When a new trip is confirmed, follow this
structure — every folder must be self-contained (own `index.html` +
`trip.css`):

```text
trips/
    hokkaido-2027/
    europe-2027/
    thailand-2026/
```

Do not scaffold a trip folder until the trip is actually confirmed.

## Next steps

1. Build out the real Tohoku itinerary page (see `build-tohoku-page.md`).
2. Add real photography to `assets/images` to replace the placeholder
   hero/card images currently sourced from picsum.photos.
3. Keep the homepage limited to trip listing only — no itinerary detail.
